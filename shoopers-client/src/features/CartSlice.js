import { createSlice } from '@reduxjs/toolkit'


const initialState = {
      value:0
}

export const cartSlice = createSlice({
      name: "product",
      initialState,
      reducers: {
            
      }
})